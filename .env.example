# Triton 模型按需加载配置
# 推荐使用 TRITON_MODELS_FILE：每行一个模型名，支持 # 注释，加载多个模型时更直观
TRITON_MODELS_FILE=/models_to_load.txt

# 也可使用英文逗号分隔的 TRITON_MODELS（优先级低于文件）
# TRITON_MODELS=CLASSIFIER_CLASSIFY_PRE_ENSEMBLE,YOLO11_DET_PRE_ENSEMBLE

# 留空或不配置上述变量时，Triton 会加载模型仓库中的所有模型（兼容旧行为）
